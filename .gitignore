node_modules/
*.tsbuildinfo

# Ergebnisse und Zwischenstände des Werkzeugs — Kundenmaterial, gehört nicht ins Repo.
ergebnisse/
.cache/

# Von `npm install` eingerichtet (uv + Docling, gemessen 1,2 GB; die Modelle
# liegen zusätzlich in ~/.cache/huggingface).
.werkzeuge/
.venv/

# Ausgabe von `npm run paket:windows` — ein rund 130 MB großes ZIP plus die
# heruntergeladenen Node- und uv-Archive. Alles davon ist reproduzierbar.
paket/

.DS_Store
